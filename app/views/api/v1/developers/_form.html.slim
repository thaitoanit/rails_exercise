= form_for @api_v1_developer do |f|
  - if @api_v1_developer.errors.any?
    #error_explanation
      h2 = "#{pluralize(@api_v1_developer.errors.count, "error")} prohibited this api_v1_developer from being saved:"
      ul
        - @api_v1_developer.errors.full_messages.each do |message|
          li = message

  .actions = f.submit
